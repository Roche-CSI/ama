model_registry_server\

steps to run the script-

=================================================
1. clone the whole repo
2. create the new environment for python = 3.10
2. # cd model_registry_mock/mr-core
3. # pip install -r requirement.txt
4. # pip install -e .
5. # cd model_registry_mock/mr-core/mr_core
6. export the variable in environment by using -  
# export FLASK_ENV=development
7. use command to run the app -
# flask run
